import { SpecialText } from "@/components/ui/special-text";

export default function Home() {
  return (
    <main className="fixed inset-0 grid place-items-center bg-black p-4 text-white">
      <SpecialText speed={18} delay={0} once className="text-xl sm:text-2xl md:text-3xl lg:text-4xl">
        work in progress portfolio for ahmed*
      </SpecialText>
    </main>
  );
}
